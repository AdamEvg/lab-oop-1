#include "tapplication.h"
#include "tpolinom.h"

int main()
{
   TApplication a;
   a.exec();
  //TPolinom C(1,1,1);
//  number * r = C.getRoots();
//  if(C.getQuantityOfRoots()==2||C.getQuantityOfRoots()==1){
//      for(int i = 0;i<C.getQuantityOfRoots();i++){
//            std::cout<<r[i]<<endl;
//      }
//  }
  //std::cout<<C.getValue(1);
// delete r;

  return 0;
}
